/**
 * No idea how everyone else wants to implement their own package, so i did this quick one.
 * string checks aren't very good. i'll think of ways it'll break as I go on.
 * so far the commands seem to be sending to the controller properly, but will need to check further.
 * 
 * 
 * 
 */

package cs350f20project.controller.cli.parser;

import cs350f20project.Startup;
import cs350f20project.controller.ActionProcessor;
import cs350f20project.controller.cli.parser.MyParserHelper;

public class tester {
	public static void main(String[] args) {
		Startup start = new Startup();
		start.go();
		
		
	}
}
